package com.jannetai.backend.entity.enums;

/** Matches complaint_appeals.status CHECK constraint (V20__create_complaint_appeals.sql). */
public enum AppealStatus {
    PENDING,
    APPROVED,
    DENIED
}
