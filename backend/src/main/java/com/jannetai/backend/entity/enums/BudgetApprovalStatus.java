package com.jannetai.backend.entity.enums;

/** Matches budget.approval_status CHECK constraint (V22__add_budget_approval_status.sql). */
public enum BudgetApprovalStatus {
    PENDING,
    APPROVED,
    REJECTED
}
