# Maven Wrapper distribution pointer. The wrapper jar itself
# (.mvn/wrapper/maven-wrapper.jar) and the mvnw/mvnw.cmd launcher scripts are
# NOT included in this deliverable: this workspace has no outbound network
# access, so they could not be downloaded and verified to actually run (see
# PHASE_HANDOFF.md). Regenerate them with a real Maven install before
# relying on `./mvnw`:
#   mvn -N wrapper:wrapper -Dmaven=3.9.9
# Until then, use a locally installed Maven 3.9+ directly.
distributionUrl=https://repo.maven.apache.org/maven2/org/apache/maven/apache-maven/3.9.9/apache-maven-3.9.9-bin.zip
wrapperUrl=https://repo.maven.apache.org/maven2/org/apache/maven/wrapper/maven-wrapper/3.3.2/maven-wrapper-3.3.2.jar
