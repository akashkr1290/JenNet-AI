# JANNet AI — Data Dictionary (Phase 2)

Authoritative source: `database/migrations/`. This document is a readable
companion, not a second source of truth — if it and the migrations ever
disagree, the migrations win. Regenerate/update this file whenever a
migration changes.

Engine: MySQL 8.0+, InnoDB, `utf8mb4` / `utf8mb4_0900_ai_ci` throughout.

---

## wards

| Column | Type | Null | Key | Notes |
|---|---|---|---|---|
| ward_id | BIGINT UNSIGNED | NO | PK | |
| name | VARCHAR(150) | NO | UQ | |
| code | VARCHAR(30) | YES | UQ | short code, e.g. `W1` |
| boundary_geojson | JSON | YES | | reverse-geocoding/admin ward editor; not consumed until a phase needs spatial queries |
| is_active | BOOLEAN | NO | | default TRUE |
| created_at / updated_at | TIMESTAMP | NO | | |

## departments

| Column | Type | Null | Key | Notes |
|---|---|---|---|---|
| department_id | BIGINT UNSIGNED | NO | PK | |
| name | VARCHAR(100) | NO | UQ | |
| description | VARCHAR(300) | YES | | |
| head_user_id | BIGINT UNSIGNED | YES | FK -> users.user_id (added in V4) | |
| is_active | BOOLEAN | NO | | |
| created_at / updated_at | TIMESTAMP | NO | | |

## users

| Column | Type | Null | Key | Notes |
|---|---|---|---|---|
| user_id | BIGINT UNSIGNED | NO | PK | |
| full_name | VARCHAR(100) | NO | | |
| mobile_number | VARCHAR(15) | NO | UQ | primary login identifier |
| email | VARCHAR(150) | YES | UQ | |
| password_hash | VARCHAR(255) | NO | | bcrypt/Argon2, hashed only |
| role | VARCHAR(30) | NO | idx | `CITIZEN, GOVERNMENT_OFFICER, DEPARTMENT_HEAD, ADMIN, SUPER_ADMIN, VERIFICATION_TEAM, MAINTENANCE_TEAM` — see decision note below |
| department_id | BIGINT UNSIGNED | YES | FK, idx | applicable to officer/dept-head/admin roles |
| ward_id | BIGINT UNSIGNED | YES | FK -> wards, idx | citizen's registered ward (corrected FK target — see decision notes) |
| reputation_score | INT | NO | | default 100, range 0–1000 |
| status | VARCHAR(20) | NO | | `ACTIVE, SUSPENDED` |
| mobile_verified_at / email_verified_at | TIMESTAMP | YES | | |
| failed_login_count | INT | NO | | supports progressive lockout (SRS 27.3) |
| locked_until | TIMESTAMP | YES | | |
| created_at / updated_at | TIMESTAMP | NO | | |

## locations

| Column | Type | Null | Key | Notes |
|---|---|---|---|---|
| location_id | BIGINT UNSIGNED | NO | PK | |
| latitude / longitude | DECIMAL(9,6) | NO | idx (combined) | |
| ward_id | BIGINT UNSIGNED | YES | FK -> wards | |
| formatted_address | VARCHAR(300) | YES | | |
| source | VARCHAR(20) | NO | | `DEVICE_GPS, EXIF, MANUAL_PIN` |
| out_of_jurisdiction | BOOLEAN | NO | | GPS Module business rule flag |
| created_at | TIMESTAMP | NO | | |

## complaints

| Column | Type | Null | Key | Notes |
|---|---|---|---|---|
| complaint_id | BIGINT UNSIGNED | NO | PK | |
| reference_number | VARCHAR(20) | NO | UQ | human-readable, e.g. `JN-2026-000123`; generated by application |
| citizen_id | BIGINT UNSIGNED | NO | FK -> users, idx | |
| category | VARCHAR(50) | NO | | `POTHOLE, GARBAGE_OVERFLOW, WATER_LEAKAGE, BROKEN_STREET_LIGHT, OPEN_MANHOLE, ILLEGAL_CONSTRUCTION, GENERAL` |
| description | VARCHAR(500) | YES | | |
| location_id | BIGINT UNSIGNED | YES | FK -> locations | |
| department_id | BIGINT UNSIGNED | YES | FK -> departments, idx | |
| assigned_officer_id | BIGINT UNSIGNED | YES | FK -> users, idx | |
| status | VARCHAR(20) | NO | idx | 12-value enum locked in Phase 1 — see decision notes |
| severity | VARCHAR(10) | YES | idx | `LOW, MEDIUM, HIGH, CRITICAL` |
| parent_complaint_id | BIGINT UNSIGNED | YES | FK -> complaints (self), idx | set when merged as a duplicate |
| corroboration_count | INT | NO | | default 1 |
| is_escalated / escalated_at | BOOLEAN / TIMESTAMP | NO / YES | | see decision notes |
| is_reopened / reopened_at | BOOLEAN / TIMESTAMP | NO / YES | | see decision notes |
| rejection_reason_code | VARCHAR(50) | YES | | |
| created_at / updated_at | TIMESTAMP | NO | idx (created_at) | |

## images

| Column | Type | Null | Key | Notes |
|---|---|---|---|---|
| image_id | BIGINT UNSIGNED | NO | PK | |
| complaint_id | BIGINT UNSIGNED | NO | FK -> complaints (CASCADE), idx | |
| image_type | VARCHAR(20) | NO | | `BEFORE, AFTER` |
| storage_key | VARCHAR(500) | NO | | S3 object key, not a public URL |
| content_type | VARCHAR(50) | NO | | `image/jpeg, image/png, image/webp` |
| file_size_bytes | INT | NO | | max 10 MB (SRS 15.1) |
| uploaded_by | BIGINT UNSIGNED | NO | FK -> users, idx | |
| uploaded_at | TIMESTAMP | NO | | |

## predictions

| Column | Type | Null | Key | Notes |
|---|---|---|---|---|
| prediction_id | BIGINT UNSIGNED | NO | PK | |
| complaint_id | BIGINT UNSIGNED | NO | FK -> complaints (CASCADE), idx | non-unique — see decision notes |
| ai_confidence | DECIMAL(5,2) | NO | | 0–100 |
| model_version | VARCHAR(30) | NO | | e.g. `YOLOv11-civic-v2.3` |
| predicted_severity | VARCHAR(10) | YES | | |
| priority_score | DECIMAL(6,2) | YES | | added vs. SRS DB table — see decision notes |
| duplicate_flag | BOOLEAN | NO | | |
| raw_model_output | JSON | YES | | full model response for audit/retraining |
| created_at | TIMESTAMP | NO | | |

## budget

| Column | Type | Null | Key | Notes |
|---|---|---|---|---|
| budget_id | BIGINT UNSIGNED | NO | PK | |
| complaint_id | BIGINT UNSIGNED | NO | FK -> complaints (CASCADE), idx | |
| estimated_cost_min / max | DECIMAL(10,2) | NO | | max >= min enforced |
| estimated_resolution_days | INT | NO | | |
| confidence_level | VARCHAR(20) | NO | | `PRELIMINARY, STANDARD, HIGH` |
| approved_by | BIGINT UNSIGNED | YES | FK -> users | |
| created_at | TIMESTAMP | NO | | |

## status_history

| Column | Type | Null | Key | Notes |
|---|---|---|---|---|
| history_id | BIGINT UNSIGNED | NO | PK | |
| complaint_id | BIGINT UNSIGNED | NO | FK -> complaints (CASCADE), idx | |
| previous_status | VARCHAR(20) | YES | | NULL for initial Draft entry |
| new_status | VARCHAR(20) | NO | | |
| actor_id | BIGINT UNSIGNED | YES | FK -> users | NULL when the AI Processing Engine is the actor |
| actor_type | VARCHAR(20) | NO | | `SYSTEM, CITIZEN, OFFICER, DEPARTMENT_HEAD, ADMIN` |
| reason | VARCHAR(500) | YES | | |
| changed_at | TIMESTAMP | NO | idx | |

## notifications

| Column | Type | Null | Key | Notes |
|---|---|---|---|---|
| notification_id | BIGINT UNSIGNED | NO | PK | |
| user_id | BIGINT UNSIGNED | NO | FK -> users (CASCADE), idx | recipient |
| complaint_id | BIGINT UNSIGNED | YES | FK -> complaints (CASCADE), idx | |
| channel | VARCHAR(20) | NO | | `IN_APP, SMS, EMAIL, PUSH` |
| message | VARCHAR(500) | NO | | |
| delivery_status | VARCHAR(20) | NO | idx | `PENDING, DELIVERED, FAILED` |
| delivery_attempts | INT | NO | | 0–3 |
| created_at | TIMESTAMP | NO | | |

## audit_logs

| Column | Type | Null | Key | Notes |
|---|---|---|---|---|
| log_id | BIGINT UNSIGNED | NO | PK | |
| actor_id | BIGINT UNSIGNED | YES | FK -> users | NULL for system-initiated actions |
| action_type | VARCHAR(50) | NO | | e.g. `ConfigChange, RoleChange, StatusOverride` |
| entity_type | VARCHAR(50) | NO | idx (with entity_id) | e.g. `Complaint, User, RoutingRule` |
| entity_id | BIGINT UNSIGNED | NO | | |
| details | JSON | YES | | before/after values |
| created_at | TIMESTAMP | NO | idx | append-only; no DELETE grant to app roles (Phase 4) |

## settings

| Column | Type | Null | Key | Notes |
|---|---|---|---|---|
| setting_id | BIGINT UNSIGNED | NO | PK | |
| scope | VARCHAR(20) | NO | idx (with scope_id) | `PLATFORM, DEPARTMENT, USER` |
| scope_id | BIGINT UNSIGNED | YES | | department_id or user_id depending on scope; app-validated |
| `key` | VARCHAR(100) | NO | UQ (with scope, scope_id) | reserved word — backtick-quote always |
| value | VARCHAR(200) | NO | | stored as string, cast per key type by application |
| updated_by | BIGINT UNSIGNED | NO | FK -> users | |
| updated_at | TIMESTAMP | NO | | |

## routing_rules

| Column | Type | Null | Key | Notes |
|---|---|---|---|---|
| routing_rule_id | BIGINT UNSIGNED | NO | PK | |
| issue_category | VARCHAR(50) | NO | idx (with is_active) | |
| department_id | BIGINT UNSIGNED | NO | FK -> departments, idx | |
| ai_confidence_threshold | DECIMAL(4,2) | NO | | 50.00–99.00, default 85.00 |
| duplicate_similarity_threshold | DECIMAL(4,2) | NO | | 50.00–99.00, default 80.00 |
| sla_hours | INT | NO | | 1–720 |
| effective_from | DATE | NO | UQ (with issue_category) | |
| is_active | BOOLEAN | NO | | |
| created_by | BIGINT UNSIGNED | NO | FK -> users | |
| created_at | TIMESTAMP | NO | | |

---

## Decision notes (things this phase changed or resolved vs. the source documents)

1. **Database engine: MySQL, not PostgreSQL.** The SRS/BRD/FRS document's own
   tech-stack section (Appendix A.2) specifies PostgreSQL, FastAPI, and
   React.js. That contradicts the stack locked in Phase 1
   (`ARCHITECTURE.md`, `README.md`): MySQL + Flyway, Spring Boot, Flutter.
   This phase was explicitly scoped as **"Phase 2 — MySQL Database +
   Migrations"**, matching the Phase 1 lock-in, so MySQL was used and the
   SRS's Postgres-specific types (`SERIAL`, `JSONB`) were translated to
   MySQL 8 equivalents (`BIGINT UNSIGNED AUTO_INCREMENT`, `JSON`). This
   contradiction is significant enough (frontend framework and backend
   framework too, not just the database) that it should be explicitly
   confirmed with the team/stakeholders before Phase 3 (Spring Boot
   backend) begins, rather than assumed resolved by this phase's default.
   See `ARCHITECTURE.md` Section 9.
2. **Users.ward_id FK target corrected.** SRS Table 19.1 says
   `ward_id INTEGER FK -> Locations.ward_id`, but `Locations`' primary key
   is `location_id`, not `ward_id` — `Wards` is the only table with that
   key. Treated as a typo in the source document and corrected to
   `wards.ward_id`.
3. **Role list expanded from Phase 1's 5-role placeholder to the SRS's
   7-role set.** `CITIZEN, GOVERNMENT_OFFICER, DEPARTMENT_HEAD, ADMIN,
   SUPER_ADMIN, VERIFICATION_TEAM, MAINTENANCE_TEAM` (SRS Section 11; the
   8th role, "AI Processing Engine", is a system actor with no user row).
   `PROJECT_INTEGRATION.md` Section 3 is updated to match in this phase.
4. **`complaints.status` keeps the 12-value enum locked in Phase 1**
   (including `ESCALATED` and `REOPENED` as status values), even though the
   SRS describes escalation/reopening as annotations on the current status
   rather than replacement states. Added `is_escalated`/`escalated_at` and
   `is_reopened`/`reopened_at` so the SRS's actual behavior is directly
   representable without discarding the locked enum. Phase 6 (Complaint
   Module) should pick one approach when it implements the real transition
   table.
5. **`predictions.priority_score` added.** The SRS's Priority Prediction
   Module (15.8) documents a numeric priority score as an output, but the
   SRS's own Predictions table (19.6) omits a column for it. Added to
   actually store the documented output.
6. **`predictions.complaint_id` is not unique**, despite the SRS's ER
   summary describing Complaints -> Predictions as 1:1, to allow the
   AI Analysis Module's documented retry behavior (15.3) to be logged.
7. **`departments.jurisdiction_id` omitted.** SRS references
   `FK -> Locations.ward_id (nullable, or a jurisdiction grouping table)` —
   an already-hedged, inconsistent target. Omitted under the SRS's own
   single-jurisdiction pilot assumption; add a real jurisdictions table if
   multi-jurisdiction support is ever exercised.
8. **`routing_rules` table added.** Not present in the SRS's Database
   Design section, but fully specified as a form (17.4) and required by
   multiple functional modules (15.7, 15.8, 15.9). Not seeded with data in
   this phase — see item 9.
9. **No seeded `routing_rules` or `settings` rows.** Both require a real
   `users.user_id` for `created_by`/`updated_by` (SRS 15.11: only Super
   Admin may create/modify these, and changes must be logged), but no user
   account exists yet — that's Phase 4's bootstrap concern. Only `wards`
   and `departments` (including a `General Triage` fallback department per
   SRS 15.7) are seeded, since the application's documented fallback
   behavior needs `General Triage` to exist as a real row.
10. **Category list includes a `GENERAL` value** not in Phase 1's original
    6-category list, to support the same fallback rule referenced in item 9.
